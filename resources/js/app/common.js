import BootstrapCollapse from '../vendor/BootstrapCollapse';
import BootstrapDropdown from '../vendor/BootstrapDropdown';
import SweetAlert from '../vendor/SweetAlert';
import GLightBox from '../vendor/GLightBox';

// Scripts that will be used globally on both front and admin panel.

// Vendor
BootstrapCollapse.init();
BootstrapDropdown.init();
SweetAlert.init();
GLightBox.init();
