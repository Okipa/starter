<li class="nav-item">
    <a class="nav-link{{ currentRouteIs('contact.page.show') ? ' active' : null }}"
       href="{{ route('contact.page.show') }}"
       title="{{ __('Contact') }}">
        {{ __('Contact') }}
    </a>
</li>
